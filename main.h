#ifndef _MAIN_H
#define _MAIN_H
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

int _printf(const char *format, ...);

int character(void);

int string(void);

int pourcent(void);

#endif
